
alias gits="git status"
alias gitd="git diff"
alias gitc="git commit -m"
alias gita="git add"
alias gitb="git checkout -b"
# this does not seem reliable
#alias gitu="git pull origin $(git rev-parse --abbrev-ref HEAD)"
alias gitm="git merge"
alias gitch="git checkout"

